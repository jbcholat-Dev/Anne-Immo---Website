# Réglages de construction du site (copier en .env pour les changer en local ; .env n'est pas commité).
# PUBLIC_INDEXATION : « oui » = pages indexables + robots.txt ouvert (production) ; toute autre valeur = noindex + robots.txt fermé (aperçu).
PUBLIC_INDEXATION=non
